// Brand X (Twitter) account constants. Mentioning the @handle in share text
// (vs. a bare #hashtag) notifies the brand account and links to its profile,
// which is what turns a share into a follow / re-engagement loop.
export const BRAND_X_HANDLE = "nbaTeamCraft";
export const BRAND_X_MENTION = `@${BRAND_X_HANDLE}`;
export const BRAND_X_FOLLOW_URL = `https://twitter.com/intent/follow?screen_name=${BRAND_X_HANDLE}`;
